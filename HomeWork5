#Напишіть функцію, що приймає один аргумант. Функція має вивести на ектан тип цього аргумента (використовуйте type)

def myself(name):

    result = type(name)

    return result

print(myself('Vlad'))
#Напишіть функцію, що приймає один аргумент будь-якого типу та повертає цей аргумент, перетворений на float.
#Якщо перетворити не вдається функція має повернути 0.


def number(a):

    try:
        result = float(a)
        return result
    except:
        print(0)

print(number(5))




#Напишіть функцію, що приймає два аргументи. Функція повинна
#якщо аргументи відносяться до числових типів - повернути різницю цих аргументів,

def ages(Bob, James):

    minus = Bob - James

    return minus

print(ages(33, 22))

#якщо обидва аргументи це строки - обʼєднати в одну строку та повернути

def car(make, model):

    name = make + model

    return name

print(car('Kia', '_Cerato'))

#якщо перший строка, а другий ні - повернути dict де ключ це перший аргумент, а значення - другий

def date(month, day):

    return {month: day}

print(date('April', 20))


#у будь-якому іншому випадку повернути кортеж з цих аргументів

def food(fruit, vegetable, snack):

    name = (fruit, vegetable, snack)

    return name

print(food('apple', 'potato', 'chips'))
